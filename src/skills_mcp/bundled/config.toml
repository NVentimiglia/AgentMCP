[paths]
skills = ".agents/skills"

# Optional: shared content folder containing a skills/ subdirectory.
# Project always wins on name collision.
# Supports absolute paths or paths relative to this config file.
# content = ".libraries"

# Legacy: shared skills directory only (use content = ... instead).
# shared_skills = ".libraries/skills"
