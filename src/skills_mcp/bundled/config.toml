[paths]
skills = "skills"
rules  = "rules"

# Optional shared content folder containing skills/ and rules/ subdirectories.
# Project files always win on name/id collision.
# Supports absolute paths or paths relative to this config file.
# content = ".libraries"

# Legacy: shared skills directory only (use content = ... instead for both skills + rules).
# shared_skills = ".libraries/skills"
